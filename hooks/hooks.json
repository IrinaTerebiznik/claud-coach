{
  "description": "Intercepts git push and PR/MR creation commands to trigger the PR Guardian checklist before submission.",
  "hooks": {
    "PreToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/pr-check.sh\""
          }
        ],
        "matcher": "Bash"
      }
    ]
  }
}
